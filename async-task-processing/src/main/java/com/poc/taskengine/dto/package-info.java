/**
 * dto — Request and response objects (Data Transfer Objects).
 * DTOs are the only objects that cross the REST boundary — the Task domain
 * model is never serialised directly to/from HTTP.
 * Populated starting Phase 3.
 */
package com.poc.taskengine.dto;
