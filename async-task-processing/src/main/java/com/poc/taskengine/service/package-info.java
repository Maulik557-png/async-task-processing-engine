/**
 * service — Business logic layer. Depends only on the TaskRepository interface,
 * never on a concrete implementation. Swapping the repository (in-memory → JPA)
 * must require zero changes here.
 * Populated starting Phase 3.
 */
package com.poc.taskengine.service;
