/**
 * controller — REST endpoints. All request/response shapes go through DTOs;
 * the domain model (Task) is never returned directly.
 * Populated starting Phase 3 (task submission endpoint).
 */
package com.poc.taskengine.controller;
